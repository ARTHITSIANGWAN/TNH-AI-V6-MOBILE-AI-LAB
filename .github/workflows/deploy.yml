name: Deploy ThitNueaHub Core

# --- [🛡️ คำสั่งยา: ยับยั้งการวิ่งแซงกัน] ---
# ถ้าเจ้านายกด Push ใหม่ ตัวเก่าที่ค้างสีฟ้าจะถูก Cancel ทันที!
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches:
      - main

env:
  # ดับคำเตือน Node 24 ตามประกาศ (บังคับใช้รันเนอร์รุ่นใหม่)
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    # เพิ่ม Timeout ป้องกันมันหมุนค้างนานเกินไป (ถ้าเกิน 10 นาทีให้ตัดจบ)
    timeout-minutes: 10 
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        id: auth
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.GCP_CREDENTIALS }}'

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Build and Deploy to Cloud Run
        run: |
          gcloud run deploy thitnueahub-mobile-ai-lab \
            --source . \
            --project thitnueahub-mobile-ai-lab \
            --region asia-southeast1 \
            --allow-unauthenticated \
            --quiet \
            --port 8080 \
            --memory 512Mi \
            --cpu 1
            
            
